
pipeline {
    agent {
        dockerfile true
    }
    stages {
        stage('Example') {
            steps {
                echo 'Hello world'
            }
        }
    }
    post {
        always {
            echo 'I will always say Hello again!'
        }
    }
}
